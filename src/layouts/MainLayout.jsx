import React from 'react';
import NavBar from '../shared/NavBar';

const MainLayout = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default MainLayout;